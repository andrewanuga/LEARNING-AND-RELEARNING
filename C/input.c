#include <stdio.h>

int main(){
    int num;
    scanf("%d", $num);
    
    printf("Your Age: %d", num);

    return 0;
}

int sum(){
    int x, y;
    scanf("%d %d", &x, &y);

    printf("%d" x+y);
    return 0;
}